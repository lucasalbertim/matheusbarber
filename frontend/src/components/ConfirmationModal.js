import React from "react"; import styled from "styled-components"; import Modal from "./Modal"; import Button from "./Button"; const ConfirmContainer = styled.div`text-align: center;`; const Message = styled.p`margin: 20px 0; font-size: 16px; color: var(--text);`; const ButtonGroup = styled.div`display: flex; gap: 12px; justify-content: center; margin-top: 24px;`; const ConfirmationModal = ({ isOpen, onClose, onConfirm, title, message, confirmText = "Confirmar", cancelText = "Cancelar", variant = "danger" }) => { return (<Modal isOpen={isOpen} onClose={onClose} title={title}><ConfirmContainer><Message>{message}</Message><ButtonGroup><Button variant="ghost" onClick={onClose}>{cancelText}</Button><Button variant={variant} onClick={onConfirm}>{confirmText}</Button></ButtonGroup></ConfirmContainer></Modal>); }; export default ConfirmationModal;
