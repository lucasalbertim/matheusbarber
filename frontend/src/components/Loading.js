import React from "react"; import styled, { keyframes } from "styled-components"; const spin = keyframes`0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); }`; const Spinner = styled.div`width: 40px; height: 40px; border: 4px solid var(--border); border-top: 4px solid var(--primary); border-radius: 50%; animation: ${spin} 1s linear infinite; margin: 20px auto;`; const Loading = ({ size = "medium", text = "Carregando..." }) => { const spinnerSize = size === "small" ? 20 : size === "large" ? 60 : 40; return (<div style={{ textAlign: "center", padding: "20px" }}><Spinner style={{ width: spinnerSize, height: spinnerSize }} /><p>{text}</p></div>); }; export default Loading;
