import React from "react"; import styled from "styled-components"; import { FaFilter, FaSearch } from "react-icons/fa"; import Input from "./Input"; const FilterContainer = styled.div`background: var(--surface); padding: 20px; border-radius: 12px; margin-bottom: 24px; box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);`; const FilterTitle = styled.h3`color: var(--primary); margin-bottom: 16px; display: flex; align-items: center; gap: 8px; font-size: 18px;`; const FilterGrid = styled.div`display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 16px;`; const FilterGroup = styled.div`display: flex; flex-direction: column; gap: 8px;`; const Label = styled.label`font-weight: 600; color: var(--text); font-size: 14px;`; const Select = styled.select`padding: 12px 16px; border: 2px solid var(--border); border-radius: 8px; background: var(--surface); color: var(--text); font-size: 16px; &:focus { outline: none; border-color: var(--primary); }`; const FilterBar = ({ filters, onFilterChange, onSearch, searchPlaceholder = "Buscar..." }) => { return (<FilterContainer><FilterTitle><FaFilter /> Filtros</FilterTitle><FilterGrid>{filters.map((filter) => (<FilterGroup key={filter.key}><Label>{filter.label}</Label>{filter.type === "select" ? (<Select value={filter.value || ""} onChange={(e) => onFilterChange(filter.key, e.target.value)}><option value="">Todos</option>{filter.options.map((option) => (<option key={option.value} value={option.value}>{option.label}</option>))}</Select>) : (<Input type={filter.type || "text"} placeholder={filter.placeholder} value={filter.value || ""} onChange={(e) => onFilterChange(filter.key, e.target.value)} />)}</FilterGroup>))}<FilterGroup><Label>Buscar</Label><Input placeholder={searchPlaceholder} onChange={(e) => onSearch(e.target.value)} /></FilterGroup></FilterGrid></FilterContainer>); }; export default FilterBar;
